const config = {
  development: {
    dialect: 'sqlite',
    storage: './db.sqlite'
  },
  test: {
    dialect: 'sqlite',
    storage: './db.sqlite'
  },
  production: {
    dialect: 'sqlite',
    storage: './db.sqlite'
  }
}

export default config
