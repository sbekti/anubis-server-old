class AuthUtility {

  static checkCredentials(nextState, replaceState) {
    console.log('Checking credentials')
    // replaceState({ nextPathname: nextState.location.pathname }, '/auth/login')
  }

}

export default AuthUtility
